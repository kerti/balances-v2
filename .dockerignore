# Keep the build context small + secret-free. backend/ and frontend/ are both
# needed (single-origin image builds the SPA too); exclude their build outputs.
.git
.github
docs
.scratch
*.log
*.md
.env
.env.example
.DS_Store
backend/bin
frontend/node_modules
frontend/dist
node_modules
