# Zone: PRESENTATION

The first **frontend-native** zone: the **client presentation + input-guardrail
layer** the non-technical audience actually touches (ADR-0021). It catalogues the
pure functions in `frontend/src/lib` that render the backend's truth without
lying to a household member, and the form-side guards that stop bad input before
it round-trips into an opaque API error. The zone **mirrors, never re-owns, the
backend truth** — FINANCE owns the net-worth number, SNAPSHOTS owns the
server-side future-date CHECK, EXPORT owns the joint-privacy rule; the rows here
pin the *client twin* of each, cross-linked, never a clone. A client-side render
or guard failing does not corrupt stored data, but it *misinforms or mis-guards*
the one reader who can't tell a display bug from a real number — which is the bar
for a row here (silent corruption or a leaked/false number, not mere mechanics).
The later rows (04+) extend the zone from pure `lib` helpers to the **component
tier** — the descriptor-driven list core (ADR-0043), verified by RTL+MSW+jsdom
suites that activate ADR-0021's deferred component tests (#69). Every row here is
verified by a vitest suite that runs in the same per-PR gate it's credited in
(`how-it-works.md`), so there is no `-strict` hazard. Source:
ADR-0021 (non-technical audience), ADR-0026 (i18n/locale). A later **E2E-native**
extension (flows a unit test can't reach, e.g. the OAuth round-trip, ADR-0024) is
seeded separately once these vitest rows land, since Playwright is subject to the
`@smoke`/nightly gate wrinkle.

| ID | Invariant | Source | Severity |
|----|-----------|--------|----------|
| INV-PRESENTATION-01 | Money never renders as a lie — `formatCurrency` (`format.ts`) shows an amount in the household locale (`en-GB` / `id-ID`), drops the fractional part for no-decimal currencies (IDR, JPY, KRW, VND) and keeps two decimals for ordinary ones, and **returns the raw input rather than `NaN`/`undefined`** when the amount isn't a number — a non-technical reader never sees a broken cell. The client twin of FINANCE's value: FINANCE-01/02 own the number, this owns its faithful display; the same NaN-safe-fallback contract holds for the sibling display helpers (`formatNumber`, `formatSignedPercent`, `roundToCurrency`). Verified in `format.test.ts` | ADR-0021 / ADR-0026 | Medium |
| INV-PRESENTATION-02 | Client date guardrail mirrors the server — `dateLimits.ts` (`thisYearMonth` / `todayDate` / `monthEndDateCapped`, and the `carryoverSeed*` clamp) caps snapshot & as-of inputs to not-future in **local** time, the form-side twin of INV-SNAPSHOTS-05 (the server's future-date 400). The guard exists so the household member is stopped in the form, not bounced by an API error they can't parse; SNAPSHOTS-05 remains the authoritative rejection, which the server now evaluates with a one-civil-day timezone tolerance (#426) so a UTC+ member's local-today never trips it — the client caps in local time, the server accepts up to that local-today, and the two agree. Verified in `dateLimits.test.ts` | ADR-0021 / INV-SNAPSHOTS-05 | Medium |
| INV-PRESENTATION-03 | Ownership label is privacy-safe — `ownershipLabel` (`ownership.ts`) renders **"Joint" with no member identity** for joint holdings (the UI face of INV-EXPORT-02's joint-is-blank-owner rule), and the owner's nickname-or-`display_name` + a "(you)" suffix for sole, degrading to a generic "Sole" when the member list is loading or the owner can't be resolved (e.g. soft-deleted user) — never leaking a name onto a holding the household owns jointly. The display twin of EXPORT-02; EXPORT-02 owns the export-side blank owner, this owns the on-screen label. Verified in `ownership.test.ts` | ADR-0021 / INV-EXPORT-02 | High |
| INV-PRESENTATION-04 | The descriptor-driven Position list core (`PositionListScreen`, ADR-0043) always renders the **Position shared surface** for every row — display name + secondary line, lifecycle status, latest-snapshot value, and the ⋮ edit/delete actions — for any position type; a concrete type is reduced to a descriptor and the core never drops a shared column or absorbs a group-specific field into it. The core also owns, type-independently, the sort toggle, the show-inactive filter, and the loading/empty/error states — so one generic replaces ten per-type screens + eleven `*ListRow` files without re-testing list behaviour per type. Verified in `PositionListScreen.test.tsx` (synthetic descriptor — asserts the abstraction, not one type's incidentals) + `descriptors/bankAccount.test.tsx` (per-type conformance over MSW) | ADR-0043 / #69 | High |
| INV-PRESENTATION-05 | The list core keeps **group extras off the compact mobile card** — the two renderers (`PositionListTable` web, `PositionListCards` mobile) consume one presentation-neutral column list; the web table shows every column, the mobile card shows the shared surface plus only the extras that opted in with `mobile: "secondary"`. A group-specific field never leaks onto the card by default, and the shared surface always survives the renderer switch (`useIsMobile`, 768px). Guards the planned dense-web / compact-mobile divergence against a hand-written second component per type. Verified in `PositionListScreen.test.tsx` | ADR-0043 | Medium |
| INV-PRESENTATION-07 | The dashboard net-worth chart's composition never misrepresents the total — the secondary lines the chart derives from a monthly report (**assets with receivables folded in**, **liabilities as a positive magnitude**, **investments**) reconcile to the net-worth line by `nw_assets + nw_receivables + nw_investments − nw_liabilities = nw_total` for every snapshot month. The fold (receivables into assets) and the positive-magnitude liabilities are presentation choices per ADR-0001's Presentation section and do **not** change the identity; the client twin of FINANCE's net-worth total (FINANCE owns the number, this owns its faithful decomposition on the home tab), sibling to INV-PRESENTATION-01. Verified in `netWorthComposition.test.ts` | ADR-0001 / ADR-0034 / INV-FINANCE-02 | High |
| INV-PRESENTATION-08 | The mobile a11y floor holds on every diverged or reflowed surface ([[adr-0050]]) — at <768px a household member can read the surface's **primary value without horizontal scroll** and every interactive control is a **≥44px tap target**. First landed on the dashboard (#507), which satisfies the doctrine's "grid → single-column stack" transform by **pure CSS reflow** (no `useIsMobile` renderer split): the header toolbar stacks, the group-breakdown label lifts above its bar+amount, and the second-currency select, `MonthPickerPopover`, `ReportPdfButton`, and rebuild links all size up to the 44px floor on phones. Renderer-independent per ADR-0050 — a truth of whichever layout is active, not of a specific renderer. Extended to the Income screen (#508), the first **bespoke** (non-descriptor) "wide table → stacked cards" renderer split: `IncomeList` picks `IncomeCard` vs `IncomeRowDesktop` at 768px, the amount is promoted to the card headline and the ⋮ action sizes to `size-11`. Extended again to the Tags breakdown (#509): `TagBreakdownSection` picks `TagBreakdownCards` vs `TagBreakdownTable` at 768px, the net value is promoted to the card headline and the pie-inclusion toggle is a `min-h-11` `<label>`; the report/management page order also flips (report first, management card last) as the shared base for the mobile layout. Extended again to the three group hubs (#510, `AssetsHome` / `InvestmentsHome` / `LiabilitiesHome`): already single-column chart stacks (only `InvestmentsHome`'s pie pair is a grid, already `md:grid-cols-2`), so like the dashboard the transform is **pure CSS reflow** — the header toolbar stacks below the title (`flex-col` → `md:flex-row`) and each `size="sm"` bulk-entry button sizes up (`h-11` → `md:h-8`); the shared chart components are unchanged. The same #510 pass also retrofits the floor onto the pre-existing [[adr-0043]] descriptor **list cards** (`PositionListCards`, all ten position types incl. receivables): the split is old (INV-PRESENTATION-05) but predated this floor, so the header action buttons are promoted to primary actions (fill the row, `size-11`) and the shared `RowActionsMenu` ⋮ trigger is prop-driven per renderer — `variant="outline"` + `size-11` on the card (matching `IncomeRowMenu`), bare `ghost` `size-8` in the table — and the shared per-currency headlines (`ListHeadline` + `InvestmentListHeadline`) stack one figure per line on mobile (`block md:inline`, `·` separator `hidden md:inline`) instead of running a mixed-currency total off the primary figure's right edge — single-currency unchanged. Extended again to the two Settings rate tables (#511, `FxRatesCard` / `InflationRatesCard`): each applies the "wide table → stacked cards" transform to its entered-rates list at 768px (the shared add form stays single-layout), the rate promoted to the card headline (`tabular-nums`) and the delete control an icon button at `size-11`; both leaves share the `fx-rate-row` / `inflation-rate-row` testids. Extended again to the ten shared-shell **detail pages** (#535, [[adr-0051]] Phase B): with all ten already on `PositionDetailScreen`, the reflow lands **once** in the core primitives rather than per page — `InfoGrid` reflows its two-column grid to stacked label/value pairs (`md:contents` dissolves the per-pair wrapper back into the grid on desktop), the header stacks (`flex-col` → `md:flex-row`) and its action row wraps, and `HistorySection` picks a stacked card list vs the wide table at 768px (`useIsMobile`) via an optional `renderCard` on the section spec — column-neutral, so a shape not yet carded falls back to the table. The **amount-only** snapshot shape rides it first: the shared `SnapshotCard` (paired with `SnapshotRow` through `useSnapshotRow` + `SnapshotRowMenu`, same `snapshot-row` / `snapshot-amount` testids) promotes the amount to the card headline and sizes the ⋮ to `size-11`, flipping all five amount-only pages (BankAccount / Property / Vehicle / Liability / Receivable); the qty×price / accrued / transaction shapes ride the same scaffold in #536 / #537 / #538. Closed out by a cross-cutting **secondary-control tap-target sweep** (#542): B1 floored only the single promoted primary action per surface, leaving the remaining `size="sm"` controls at ~32px on phones — the detail snapshot-section header (Export / Import / carryover) and actions row (back / Help / Edit / Terminate / Delete), the Income regularity filter, and the Settings Erase / Reactivation / Invite secondary buttons — so each now carries `min-h-11 md:min-h-0` (≥44px below 768px, natural `size-sm` from 768px up); the same pass fixed a B1 gap where the **primary** qty×price / accrued-interest snapshot triggers were themselves left at `size="sm"`, promoting them to the `h-11 md:h-8` primary floor already worn by the amount-only `CreateSnapshotDialog`. #542 also gives both section headers a **deliberate two-row layout** (stacked on phones, inline from 768px): the snapshot header splits **create** (`snapshot-create-row` — Copy carryover + New) from **I/O** (`snapshot-io-row` — Export + Import, `renderImportControl` split out of `renderCreateControls`), with **Copy carryover promoted primary** (solid + `h-11 md:h-8`, since "same as last month" is the common month-close) and New demoted to the secondary outline floor unless it stands alone on the first-ever snapshot; the transactions header (stock / mutualFund / gold / bond) splits **trades** (`txn-trades-row` — Buy primary, Sell, plus Maturity for bond) from **cash flows** (`txn-cashflow-row` — dividend/distribution/coupon income + fees); the same treatment reaches the position **actions row** (Help + Edit above, Close/Status + Delete below). On phones every one of these rows — snapshot, transaction, and actions — goes **full width with its buttons splitting the width evenly** (`max-md:[&>*]:flex-1`), collapsing back to natural inline widths from 768px up (the shared `HistorySection` header wrapper carries `max-md:w-full` so the nested rows can stretch past its `items-start` column). The same treatment reaches the investment-subtype **list filter**: the `RiskProfileFilter` chip bar fills the width and splits its four options evenly on phones (each clearing the 44px floor), and the "show closed positions" toggle drops **below** the chips (`flex-col` → `md:flex-row md:justify-between`) instead of sharing their row. Verified in `dashboard-mobile.spec.ts`, `income-mobile.spec.ts`, `tags-mobile.spec.ts`, `hubs-mobile.spec.ts`, `list-mobile.spec.ts`, `settings-rates-mobile.spec.ts` and `detail-mobile.spec.ts` / `detail-mobile-qtyprice.spec.ts` / `detail-mobile-accrued.spec.ts` / `detail-mobile-transactions.spec.ts` (@smoke, 390px) + `IncomeList.test.tsx` / `TagBreakdownSection.test.tsx` / `FxRatesCard.test.tsx` / `InflationRatesCard.test.tsx` / `HistorySection.test.tsx` / `detail-mobile.test.tsx` / `detail-mobile-qtyprice.test.tsx` / `detail-mobile-accrued.test.tsx` / `detail-mobile-transactions.test.tsx` / `RiskProfileFilter.test.tsx` (renderer conformance). | ADR-0050 / ADR-0051 / #507 / #508 / #509 / #510 / #511 / #535 / #536 / #537 / #538 / #542 | High |
| INV-PRESENTATION-06 | The shared Position Create/Edit dialog scaffold (`PositionFormDialog`, ADR-0043 follow-on / #334) owns the plumbing every per-type dialog duplicated: the dialog shell + header, the `<form>` wrapper, the **shared error envelope** (`errorMessage`, the ADR-0027 face), and the footer (cancel + submit with a distinct pending label + disabled-in-flight). It exposes two modes — **create** (self-triggering, owns its own open state, resets via `onClosed` on close) and **edit** (controlled via `open`/`onOpenChange`, no trigger) — and hands the caller a `close` callback through `onSubmit(close)` so the type keeps its own payload mapping and submit guards while the scaffold owns dismissal. The field **body stays hand-written** per type (no field-config DSL); the dirty-guard is deliberately out of scope. Verified in `PositionFormDialog.test.tsx` (synthetic body — asserts the shell contract, not one type's fields) | ADR-0043 / #334 | Medium |
